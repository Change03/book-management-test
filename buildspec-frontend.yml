version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20

  pre_build:
    commands:
      - cd book-management-front
      - npm ci

  build:
    commands:
      - npm run build
      - ls -la

artifacts:
  files:
    - '**/*'