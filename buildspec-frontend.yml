version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20

  pre_build:
    commands:
      - echo "프론트엔드 의존성 설치"
      - cd book-management-front
      - npm ci

  build:
    commands:
      - npm run build

artifacts:
  files:
    - '**/*'
  base-directory: book-management-front